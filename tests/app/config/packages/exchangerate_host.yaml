exchangerate_host:
    cache:
        # Test default values

when@test:
    exchangerate_host:
        cache:
            pool: 'exchangerate_host.cache'
            latest_rates_expiration: 'tomorrow 6am'
            convert_currency_expiration: 'tomorrow 6am'
            historical_rates_expiration: 86400
            timeseries_rates_expiration: 'tomorrow 6am'
            fluctuation_data_expiration: 86400
            supported_currencies_expiration: '+2days'
            eu_vat_rates_expiration: 'tomorrow 6am'

when@test_with_no_cache:
    exchangerate_host:
        cache:
            pool: false

when@test_with_invalid_cache:
    exchangerate_host:
        cache:
            pool: 'wrong_cache'
